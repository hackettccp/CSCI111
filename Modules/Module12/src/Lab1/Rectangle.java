package Lab1;

/**
 * Rectangle Object.
 * 
 * Create three fields: length (int), width (int), and color (String).
 * Do not give the fields default/starting values.
 * 
 * Create four constructors:
 * One with no arguments, 
 * One with one argument (sets length),
 * One with two arguments (sets length and width), 
 * One with three arguments (sets length, width, and color)
 * 
 * The default values to give the fields in the constructors are:
 * length and width = 1, color = "red"
 * 
 */
class Rectangle {
    
    
    
}
